{
  "name": "serviceverse",
  "version": "1.0.0",
  "description": "ServiceVerse - Multi-utility SaaS platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend/functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run serve --workspace=backend/functions",
    "emulators": "npm run build --workspace=backend/functions && npx firebase emulators:start",
    "emulators:functions": "npm run build --workspace=backend/functions && npx firebase emulators:start --only functions",
    "deploy:rules": "bash scripts/deploy-rules.sh",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend/functions",
    "build": "npm run build:frontend",
    "install:all": "npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
